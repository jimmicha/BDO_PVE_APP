import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.jimmicha.bdocompanion', appName: 'BDO Companion', webDir: 'dist', server: { androidScheme: 'https' }, android: { backgroundColor: '#131614' } };
export default config;
