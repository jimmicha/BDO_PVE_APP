BDO Companion beta - implementation patch
Date: 2026-09-08

UPDATE 2026-09-10
- Imported the new 70-step / 23-item ladder as an admin-editable draft.
  Preserved existing milestone and family claim identities; review dates stay
  unset. New conversion, set-reward and cost semantics need further review.
- 15 core tests pass. Android debug APK generated and signature verified;
  real-device and Play release testing remain outstanding.
- First migration installed on the replacement backend. Remaining setup and
  live cutover are pending. See docs/COMPLETION_REVIEW.md for current status;
  the original DONE/LEFT sections below describe the earlier checkpoint.

DONE
- React/TypeScript web app: accounts, onboarding, characters, gear, resources,
  roadmaps, reservations, transaction previews, journal, settings and export.
- Supabase migrations with ownership checks, RLS, exact quantities, revision
  conflicts, idempotent transactions, six family claims and account deletion.
- Reviewed HYPERBOOST accessory catalog with admin review/publish/rollback.
- Imported 250 gear-stat rows and 12 guide sections; comparisons preserve
  unknown values and show 13 review flags. Imported mechanics remain unreviewed.
- Offline read-only behavior, cache clearing, keyboard skip link, contrast fixes
  and larger touch targets. 14 core tests and 8 browser tests pass.
- Web deployed at https://www.blackdesertcompanion.app.
- Android project, assets, return links, build script and CI workflows added.
  Java 21 and Android tools installed locally; debug APK build not yet verified.
- Encrypted backup/restore scripts, deletion replay and gated daily CI workflow.
- New backend taxhvwhachtrhtcdbnsz selected; public config prepared, canonical
  URL and eight web/Android callbacks saved. OAuth reconnection succeeded.

LEFT TO DO
- Reload Codex's connector to use the refreshed Supabase authorization; install
  migrations/catalog/deletion function on the new backend and run hosted tests.
- Switch and redeploy the live app only after new-backend verification. Current
  live build still targets the previous backend. No player data was migrated.
- Configure Google OAuth, SMTP and signup hook; test real email/Google flows.
- Finish Android compilation and real-device journeys, signing and Play testing.
- Configure separate staging, backup secrets/schedule/alerts and perform an
  actual hosted restore rehearsal. CI workflow files alone are not active jobs.
- Finalize privacy/operator details and complete manual accessibility review.

No credentials, downloaded toolchains, private test accounts or build outputs
are included in this commit. No paid service or store purchase was made.
- Administrator draft import/save verified in desktop and mobile browsers (2 passed).

CONTINUATION CHECKPOINT 2026-09-10
- Added manual equipment upgrade costs with preview, pinned revision, exact deductions and a single atomic journal transaction. Existing equipment IDs are retained.
- Added backend capability gating so older servers do not offer unsupported deductions.
- Fixed local startup to apply later migrations without resetting saved data; added checksum, rollback and legacy schema recognition checks.
- Validation: 18 core tests and production build passed; all 12 desktop/mobile browser journeys passed, and both upgrade journeys passed again after the final capability guard. See NEXT_STEPS.md for the remaining release plan.
- Not deployed: hosted connector unavailable, new backend installation incomplete. Guided conversions, catalog review, real auth, Android release and operating backups remain.

CONTINUATION CHECKPOINT 2026-09-10 (guided equipment conversions)
- Implemented NEXT_STEPS.md item 3: catalog steps can now declare an optional
  reward.source (item_key, enhancement range, keep_assignment) to consume and
  transform an existing owned equipment item in place, instead of only ever
  minting a new one. Plain acquire rewards are unchanged.
- Added app_private.convert_equipment, spliced into companion_command's
  step_complete branch alongside the existing resource-cost transaction, and
  a chained guided_conversions_v1 capability advertisement.
- Extended validate_catalog to check conversion source semantics; extended
  Roadmap's completion dialog with a source-item picker and preview, and
  fixed a pre-existing bug where a reward-only step with no resource cost
  could never reach spend mode.
- New tests/equipment-conversions.test.ts covers ownership, compatibility,
  reuse rejection, atomicity, reservation-caused rollback, retry, reopen,
  two-slot precision and catalog migration. 19 core tests, typecheck,
  production build and all 12 desktop/mobile browser journeys pass.
- Not deployed: this is local-migration and frontend work only. Hosted
  cutover (item 1), catalog game-data review (item 4), real auth (item 5)
  and release/ops (item 6) remain, per NEXT_STEPS.md.

CONTINUATION CHECKPOINT 2026-09-10 (hosted backend cutover, item 1)
- Supabase connector authorized. Confirmed only the core migration was
  installed on taxhvwhachtrhtcdbnsz; applied the remaining nine migrations
  in order (catalog, API hardening/deletion, both foreign-key index
  batches, PT409 error codes, equipment upgrade costs and its capability,
  and all three guided-conversion migrations from this session).
- Installed the reviewed v1 catalog seed (Alustin's Kharazad journey,
  published) and an admin beta_members allowlist entry.
- Deployed the delete-account Edge Function with its import map and JWT
  verification enabled.
- Ran security and performance advisors: only informational findings on
  intentionally policy-less app_private tables (no direct client access;
  everything routes through SECURITY DEFINER RPCs) and brand-new unused
  indexes. Nothing actionable.
- Read-only preflight against the live REST API confirms: email enabled,
  Google disabled, email confirmation required, companion_snapshot RPC
  installed and reachable, anonymous access denied.
- Not done: Google OAuth, SMTP, Before User Created hook and the
  hosted two-account acceptance suite (item 2) are still outstanding.
  .env.local and the live deployment still point at the previous backend;
  no traffic has been switched and no player data was touched.
