# Public browser configuration. Never add a service-role or secret key here.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=replace_with_local_supabase_publishable_key
VITE_APP_URL=http://localhost:5173
# Set only for the isolated development server. Production builds reject this flag.
# VITE_LOCAL_TEST_SERVER=true
# Backend and CI secrets are documented in docs/OPERATIONS.md, never VITE_*.
